import 'category.dart';

class ReadingCategory extends Category {
  ReadingCategory({
    super.name = 'Reading',
    required super.description,
    required super.image,
    required super.questions,
  });
}
